{
  "installCommand": "npm install --legacy-peer-deps --no-audit --no-fund && npm install --prefix seniorease-project --legacy-peer-deps --no-audit --no-fund",
  "buildCommand": "npm run build --prefix seniorease-project",
  "outputDirectory": "seniorease-project/.next",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/facebook-post",
      "schedule": "0 8 * * *"
    }
  ]
}
