import { CLIENT_PAGE_METADATA } from '@/lib/seo';

export const metadata = CLIENT_PAGE_METADATA['zo-werkt-het'];

export default function ZoWerktHetLayout({ children }: { children: React.ReactNode }) {
  return children;
}
