import { CLIENT_PAGE_METADATA } from '@/lib/seo';

export const metadata = CLIENT_PAGE_METADATA.voorwaarden;

export default function VoorwaardenLayout({ children }: { children: React.ReactNode }) {
  return children;
}
