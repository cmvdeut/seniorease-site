import { CLIENT_PAGE_METADATA } from '@/lib/seo';

export const metadata = CLIENT_PAGE_METADATA.kalender;

export default function KalenderLayout({ children }: { children: React.ReactNode }) {
  return children;
}
