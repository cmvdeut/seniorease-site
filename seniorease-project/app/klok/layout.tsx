import { CLIENT_PAGE_METADATA } from '@/lib/seo';

export const metadata = CLIENT_PAGE_METADATA.klok;

export default function KlokLayout({ children }: { children: React.ReactNode }) {
  return children;
}
