import { CLIENT_PAGE_METADATA } from '@/lib/seo';

export const metadata = CLIENT_PAGE_METADATA.puzzels;

export default function PuzzelsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
