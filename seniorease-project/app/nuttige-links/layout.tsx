import { CLIENT_PAGE_METADATA } from '@/lib/seo';

export const metadata = CLIENT_PAGE_METADATA['nuttige-links'];

export default function NuttigeLinksLayout({ children }: { children: React.ReactNode }) {
  return children;
}
