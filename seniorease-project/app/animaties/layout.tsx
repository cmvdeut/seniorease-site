import { CLIENT_PAGE_METADATA } from '@/lib/seo';

export const metadata = CLIENT_PAGE_METADATA.animaties;

export default function AnimatiesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
