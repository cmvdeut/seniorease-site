import { CLIENT_PAGE_METADATA } from '@/lib/seo';

export const metadata = CLIENT_PAGE_METADATA.afvinken;

export default function AfvinkenLayout({ children }: { children: React.ReactNode }) {
  return children;
}
