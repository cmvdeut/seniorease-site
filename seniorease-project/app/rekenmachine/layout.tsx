import { CLIENT_PAGE_METADATA } from '@/lib/seo';

export const metadata = CLIENT_PAGE_METADATA.rekenmachine;

export default function RekenmachineLayout({ children }: { children: React.ReactNode }) {
  return children;
}
