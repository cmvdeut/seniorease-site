import { CLIENT_PAGE_METADATA } from '@/lib/seo';

export const metadata = CLIENT_PAGE_METADATA.bibliotheek;

export default function BibliotheekLayout({ children }: { children: React.ReactNode }) {
  return children;
}
